import { createStore } from 'vuex'
import { tableList } from './tableList.js'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: { tableList }
})
